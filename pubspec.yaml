name: shared_utils
description: "مكتبة أدوات مشتركة للمشاريع - تتضمن معالجة API، extensions، formatters، وأدوات مساعدة"

version: 2.1.1

homepage: https://github.com/AbberApp/shared_utils

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Network
  dio: ^5.9.0
  connectivity_plus: ^7.0.0

  # File & Image Pickers
  file_picker: ^10.3.7
  image_picker: ^1.2.1
  image_cropper: ^11.0.0

  # UI
  fluttertoast: ^9.0.0

  # Utils
  intl: ^0.20.2
  url_launcher: ^6.3.2
  ua_client_hints: ^1.4.1

  # Device Info
  device_info_plus: ^12.3.0
  package_info_plus: ^8.3.0
  persistent_device_id: ^1.1.0

  # App Update
  in_app_update: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
